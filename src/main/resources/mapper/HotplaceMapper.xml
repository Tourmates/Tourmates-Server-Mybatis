<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.tourmates.hotplace.repository.HotplaceMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into hot_place(tag, title, content, visited_date, upload_file_name, store_file_name)
        values (#{tag}, #{title}, #{content}, #{visitedDate}, #{uploadFileName}, #{storeFileName});
    </insert>

    <select id="findById">
        select *
        from hot_place
        where hot_place_id = #{id};
    </select>

    <select id="findAll">
        select *
        from hot_place;
    </select>

    <update id="update">
        update hot_place
        set tag              = #{tag},
            title            = #{title},
            content          = #{content},
            hit              = #{hit},
            vote             = #{vote},
            visited_date     = #{visitedDate},
            upload_file_name = #{uploadFileName},
            store_file_name  = #{storeFileName}
        where hot_place_id = ${id};
    </update>

    <delete id="remove">
        delete
        from hot_place
        where hot_place_id = #{id};
    </delete>
</mapper>